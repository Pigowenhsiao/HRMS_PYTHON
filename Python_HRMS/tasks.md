# Python HRMS 任務清單（SQLite 版）

## 立即（Now）
- 修正主畫面亂碼並優化布局（已完成）；統一中文字型與 UTF-8。
- 建立 SQLite schema 與初始化腳本（含員工、個資、學歷、證照、訓練紀錄、對應、權限、部門/區域/職務）。
- 建立 DAO 介面與資料模型；以參數化查詢實作 SQLite CRUD。
- 建立 Access → SQLite 匯入腳本，對齊欄位並做筆數/必填驗證。
- 建立設定檔樣板 config.json/.env（DB 路徑、匯出路徑、版本、權限選項）。
- 內建預設帳號：SQLite 初始化時插入 `admin` / `admin123`，部署後需強制更改密碼或改用雜湊存放。
- 主畫面多語切換：詞條集中管理（繁中/英文/日文），控件文字依選擇更新。

## 短期（Next）
- 完成員工/個資/學歷 CRUD 與查詢 UI（PyQt5），串接 DAO；對應多語詞條。
- 完成證照設定、訓練紀錄、機台對應 CRUD 與查詢；新增/修改後支援 CSV 匯出（失敗改寫本機並提示），匯出訊息/欄位支援多語。
- 加入權限檢查：登入帳號 vs authority 表；未授權隱藏/鎖定按鈕。主畫面載入時讀設定檔與版本號，驗證 `software`/`authority`。
- 到期檢核與報表視窗：依天數/部門篩選，提供 CSV 匯出；報表欄位與標題支援多語。
- 多語資源檔：將詞條抽離為 JSON/ini/yaml，UI 與報表/匯出共用，支援繁中/英文/日文；提供翻譯審校流程。

## 中期（Later）
- 單元測試覆蓋 DAO/service；UI 冒煙測試腳本。
- 錯誤提示與日誌格式化；匯出失敗的重試/回退策略。
- 打包與部署腳本（PyInstaller），附預設 SQLite DB 與設定檔；撰寫使用手冊（含多語）。
