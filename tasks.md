# Python HRMS 任務清單（SQLite 版）

## 立即（Now）
- 版號 `v0.3.1`：主畫面語言切換/字級/全螢幕；版本顯示同步 config；狀態列顯示 DB 路徑；登入權限控制按鈕可見。
- 多語：繁中/日/英覆蓋主畫面與子畫面（員工、個資、學歷、證照設定/機台對應、訓練紀錄、到期檢核、區域/部門/職務、權限）。
- 資料聯動：證照設定的部門改為下拉，資料來自部門維護；訓練/證照紀錄新增時查重，重複則改為更新並提示。
- DB/腳本：SQLite `./data/hrms.db`；scripts/load_sample 產生示範資料與 admin/pigo（密碼為 md5(admin123)）。
- 設定：權限管理可設定 DB 路徑並建立新 DB（預設 HRMS_Database.db），更新後需重啟；訊息多語化。
- 安全：登入必須；權限表含密碼與各功能許可；未授權按鈕隱藏。

## 短期（Next）
- 全面複核子畫面表頭/標籤是否仍有硬編碼或亂碼，逐一修正。
- 報表/匯出：補充欄位選擇與多語表頭，CSV 匯出失敗時提供友善提示。
- 權限 UI：表格勾選與下方勾選保持同步，考慮批量啟用/停用。

## 中期（Later）
- 測試：DAO/Service 單元測試；關鍵 UI 冒煙測試。
- 提醒/通知：到期檢核與訓練提醒最佳化。
- 打包：PyInstaller，附 SQLite 與設定檔；若需 MySQL/網路路徑，提供設定範本與操作指引。
